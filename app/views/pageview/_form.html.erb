<%= form_for @pageview, :html => { :class => 'form-horizontal' } do |f| %>
  <div class="control-group">
    <%= f.label :post_id, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :post_id, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :user_ip, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :user_ip, :class => 'text_field' %>
    </div>
  </div>

  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                pageview_path, :class => 'btn' %>
  </div>
<% end %>
