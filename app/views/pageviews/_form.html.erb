<%= form_for(@pageview) do |f| %>
  <% if @pageview.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pageview.errors.count, "error") %> prohibited this pageview from being saved:</h2>

      <ul>
      <% @pageview.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :post_id %><br>
    <%= f.number_field :post_id %>
  </div>
  <div class="field">
    <%= f.label :user_ip %><br>
    <%= f.text_field :user_ip %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
